{
  "skill_name": "cross-compat-check",
  "evals": [
    {
      "id": 1,
      "prompt": "I wrote a skill called release-notes-drafter (it's at /home/claude/fixtures/bad-skill) and I want to check it into GitHub for my team to use from both Claude Code and Cowork. Can you check it for anything that would break or behave differently between the two before I push it? Walk me through what you find -- I don't have any experience with this, so explain why things are a problem, not just what to change. Since I'm not available to answer follow-up questions right now, assume I approve every fix you'd reasonably recommend, apply them, and give me a final summary of everything you changed and why.",
      "expected_output": "Should run the scanner against /home/claude/fixtures/bad-skill, walk through each finding (frontmatter fields argument-hint/disable-model-invocation/model, the !`git log` shell injection, ${CLAUDE_PROJECT_DIR} and ${CLAUDE_SKILL_DIR} substitutions, the 'Task tool' subagent-name trap, AskUserQuestion reference, hardcoded /Users/alice/... path, 'outputs directory' phrasing, and the unguarded env var read in scripts/format.py), explain the risk for each in plain terms, apply fixes, add/update a compatibility field if warranted, and end with a summary. A final re-scan should show the fixed findings gone.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "Before I share this on GitHub, can you double check my skill for cross-platform issues? It's the release-notes-drafter skill at /home/claude/fixtures/good-skill.",
      "expected_output": "Should run the scanner, get zero findings, and say plainly that the skill looks portable already rather than inventing issues to justify the check. Should not modify any files.",
      "files": []
    },
    {
      "id": 3,
      "prompt": "Can you run a compatibility check on my skill before I check it in? It's at /home/claude/fixtures/cowork-only-skill -- it's a desktop file organizer that uses the remote-devices bridge and AskUserQuestion. I know it's Cowork-only, that's intentional, I just want to make sure I haven't accidentally left in anything broken or that I'm not missing anything I should document.",
      "expected_output": "Should run the scanner, recognize the AskUserQuestion / device-bridge references are intentional (matches the user's stated intent and the skill's own compatibility field already documents the Cowork-only requirement), not push unnecessary rewrites to make it 'portable', and instead confirm the compatibility field accurately reflects the limitation. Should surface any other real finding (if any) but not manufacture busywork.",
      "files": []
    },
    {
      "id": 4,
      "prompt": "Can you check my plugin's skill for cross-compatibility before I ship it? The skill is at /home/claude/fixtures/plugin-fixture/skills/sync-data -- it's part of a bigger plugin (there's a hooks/ and agents/ folder alongside the skills/ folder). It syncs data using an API token and a deploy secret it reads from the environment.",
      "expected_output": "Should run the scanner, correctly walk through the plugin-level findings (hooks/hooks.json present -- verify it works in Cowork; agents/ present -- same; and the hardcoded /home/xander/... path inside hooks/hooks.json, which should be flagged as confirmed and fixed to use ${CLAUDE_PLUGIN_ROOT} instead). Should also flag the undocumented DEPLOY_SECRET env var (read via both os.getenv() in sync.py and $DEPLOY_SECRET in sync.sh) but should NOT flag SYNC_API_TOKEN, since that one is already documented in the skill's own compatibility field -- this is the key behavior being tested. Should not flag $HOME in sync.sh.",
      "files": []
    },
    {
      "id": 5,
      "prompt": "Can you check this skill for cross-compatibility? It's at /home/claude/fixtures/mcp-env-fixture -- it calls an MCP tool directly by name and loads credentials with python-dotenv, and there's a references/setup.md with more setup instructions.",
      "expected_output": "Should run the scanner and walk through all 6 findings: the hardcoded mcp__acme-crm__list_contacts reference in SKILL.md (explain that the server-name segment isn't portable across users/sessions/orgs and propose describing the capability + resolving the tool at runtime via ToolSearch/the loaded MCP list instead), the python-dotenv import and load_dotenv() call in scripts/loader.py (explain it's an extra pip dependency not guaranteed present on either product and propose the stdlib-only .env loader pattern from compat-rules.md rule 10), the undocumented ACME_API_KEY env var read, and -- this is the key behavior being tested -- the two findings that live in references/setup.md rather than SKILL.md itself (the hardcoded /home/alice/... absolute path, and the AskUserQuestion reference), proving the scanner checks reference docs too, not just the entry-point file. Should NOT propose a compatibility-field fix for the MCP-hardcode or dotenv findings, since those need an actual rewrite, not documentation.",
      "files": []
    },
    {
      "id": 6,
      "prompt": "Check my skill at /home/claude/fixtures/exec-fixture before I share it -- it has three bundled scripts.",
      "expected_output": "Should run the scanner and report the verification summary before the findings list (e.g. '3 scripts checked, 2 syntax-ok, 1 syntax-failed, 2 imports probed, 2 failed, 1 guarded'), then walk through exactly 2 confirmed findings: the real syntax error in scripts/broken.py (an unclosed paren -- reproduced via python3 -m py_compile, not inferred) and the genuinely missing import in scripts/missing_dep.py (definitely_not_a_real_package_12345, reproduced via a real `python3 -c \"import ...\"` attempt). Should explicitly NOT flag the import in scripts/guarded_optional.py (definitely_not_a_real_package_98765) as a finding, since it's wrapped in try/except ImportError with a working fallback -- that's the key behavior being tested: the scanner actually ran the import check (it shows up in the verification summary's imports_guarded list) but correctly withheld a finding because the code already handles the failure gracefully. Both real findings should be presented as confirmed, reproduced facts (not heuristics), and described as bugs that break identically on both products, not primarily a cross-compat issue.",
      "files": []
    }
  ]
}
